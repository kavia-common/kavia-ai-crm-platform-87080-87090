# PUBLIC_INTERFACE
# Example environment configuration for CRM frontend.
# Copy to .env and set REACT_APP_API_BASE to your backend API base URL.
# If not set, the app defaults to http://localhost:8000/api for development.
REACT_APP_API_BASE=https://your-backend.example.com/api
